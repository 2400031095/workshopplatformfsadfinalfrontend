# Optional for deployed frontend or when not using Vite proxy
# Example: https://your-backend.onrender.com
VITE_API_BASE_URL=

# Local frontend-only testing mode (no backend required)
# true = use in-browser mock API
# false = use real backend API
VITE_USE_MOCK_API=true
